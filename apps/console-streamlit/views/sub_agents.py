from views._placeholders import render_placeholder

render_placeholder(
    icon="🤖",
    title="Sub-Agents",
    subtitle="Tier-2 sub-agent registry + per-agent capability + permission scope.",
    deferred_to="Loop 9+",
    items=[
        "Agent roster (Mark / Tom / Hank / Paul / PM-α …)",
        "Per-agent permission set + adapter allowlist",
        "Health + last-invoked telemetry",
        "Enable/disable per tenant",
    ],
)
