"""IWO3 Python contract mirror — enums + API-boundary shapes.

Loop 5 Phase 5.2 ships the enum + shape mirror; Loop 7 imports it for
FastAPI response serialisation. Parity with the TS canonical surface
is enforced at CI time by ``tests/contract/enum-parity.test.ts``.
"""
